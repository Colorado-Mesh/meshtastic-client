diff --git a/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js b/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
index d23a542..865caca 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
@@ -15,7 +15,8 @@ var LogMessageByKey;
     LogMessageByKey["PKG_COLLECTOR_OUTPUT"] = "collector stderr output";
 })(LogMessageByKey || (exports.LogMessageByKey = LogMessageByKey = {}));
 exports.logMessageLevelByKey = {
-    [LogMessageByKey.PKG_DUPLICATE_REF]: "info",
+    // Hoisted npm tree lists many edges to the same copy; not actionable — avoid noisy dist output
+    [LogMessageByKey.PKG_DUPLICATE_REF]: "debug",
     [LogMessageByKey.PKG_NOT_FOUND]: "warn",
     [LogMessageByKey.PKG_NOT_ON_DISK]: "warn",
     [LogMessageByKey.PKG_SELF_REF]: "debug",
diff --git a/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js b/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
index 0956d26..4718c45 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
@@ -312,10 +312,13 @@ class NodeModulesCollector {
         }
         await new Promise((resolve, reject) => {
             const outStream = (0, fs_extra_1.createWriteStream)(tempOutputFile);
+            // shell: true with an args array triggers Node DEP0190; on Unix npm is invoked
+            // without shell so argv are passed safely. Windows .cmd path above still uses cmd.exe.
+            const shell = isWindowsScriptFile || process.platform === "win32";
             const child = childProcess.spawn(command, args, {
                 cwd,
                 env: { COREPACK_ENABLE_STRICT: "0", ...process.env }, // allow `process.env` overrides
-                shell: true, // `true`` is now required: https://github.com/electron-userland/electron-builder/issues/9488
+                shell, // true required on Windows: https://github.com/electron-userland/electron-builder/issues/9488
             });
             let stderr = "";
             child.stdout.pipe(outStream);
diff --git a/node_modules/app-builder-lib/out/node-module-collector/npmNodeModulesCollector.js b/node_modules/app-builder-lib/out/node-module-collector/npmNodeModulesCollector.js
index af047a9..575168d 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/npmNodeModulesCollector.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/npmNodeModulesCollector.js
@@ -11,6 +11,8 @@ class NpmNodeModulesCollector extends nodeModulesCollector_js_1.NodeModulesColle
             manager: packageManager_js_1.PM.NPM,
             lockfile: "package-lock.json",
         };
+        // Dedupe duplicate-ref log: npm list emits one node per hoisted edge; log each package@version once.
+        this._duplicateRefLogged = new Set();
     }
     getArgs() {
         return ["list", "-a", "--include", "prod", "--include", "optional", "--omit", "dev", "--json", "--long", "--silent", "--loglevel=error"];
@@ -26,7 +28,11 @@ class NpmNodeModulesCollector extends nodeModulesCollector_js_1.NodeModulesColle
                 if (!this.allDependencies.has(childDependencyId)) {
                     this.allDependencies.set(childDependencyId, pkgOverride);
                 }
-                this.cache.logSummary[moduleManager_js_1.LogMessageByKey.PKG_DUPLICATE_REF].push(childDependencyId);
+                // Only log each package@version once — npm tree lists many edges to the same hoisted copy
+                if (!this._duplicateRefLogged.has(childDependencyId)) {
+                    this._duplicateRefLogged.add(childDependencyId);
+                    this.cache.logSummary[moduleManager_js_1.LogMessageByKey.PKG_DUPLICATE_REF].push(childDependencyId);
+                }
                 continue;
             }
             // Always store this dependency and recurse into its children
