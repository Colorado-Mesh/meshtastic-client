#!/bin/sh
# Run format check, lint, and tests before every commit.
# To skip in an emergency: git commit --no-verify

npm run format:check || { echo "Run 'npm run format' to fix formatting."; exit 1; }
npm run lint || exit 1
npm run test:run
